#include<iostream>
#include<list>
using namespace std;
int main (){


//another stl is for  the queue
/*
it follows the FIFO that means to say that first in and the first out  data structure
its implementation is by the help of using other containers e.g. deque ,list as the underlying storage
jo phale aayega vhi phaleh hi remove hoke jayega bad vala phale nhi ja sakta

*/




//     //now we are at the list 
//     //this is used to store the data at non continious areas and can only be accesed. using the address or we can  say by the pointers


//     //linked list is connected in the form of nodes that are used in the doubly linkded list

//  list<int> myList;



// //insertiion
// myList.push_back(10);
// myList.push_back(20);
// myList.push_back(30);
// myList.push_back(40);
// //10 20 30 40
// myList.push_front(100);
// // 100 10 20 30 40
// myList.pop_back();
// // // 100 10 20 30
// myList.pop_front();
// // //10 20 30


// cout << myList.size();
// cout << endl;
// myList.clear();
// cout << myList.size() << endl;

    return 0;
}