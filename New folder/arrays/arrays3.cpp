#include<iostream>
#include<vector>
using namespace std;



int main(){

    //declare  2D array
    // int arr[4][3];

    // initialsie
    // int arr[3][3] = {
    //     {10,20,100},
    //     {30,40,200},
    //     {50,60,300}
    // };




    //this is the mehrod to print the whole array in a form


    // cout << arr[1][1] << endl;


    // //trAVERSE INTO THE WHOLE ARRAY
    // int rowSize = 3;
    // int colSize = 2;


    //diagonal matrix


    //another method to print the array


    // for( int rowIndex = 0 ; rowIndex < rowSize ; rowIndex++ ){
    //     for(int colIndex = 0 ; colIndex < colSize;  colIndex++){
    //         cout << arr[rowIndex][colIndex] << " " ;
    //     }
    //     cout << endl;
    // }


    
    //printing column wise
    // for (int col = 0;col <colSize; col++){
    //     for (int row = 0; row < rowSize; row++){
    //         cout << arr[row][col] << " ";
    //     }
    //     cout << endl;
    // }


//keval static k case mai garbage value dekhne ko milti h 
//dynamic array k case mai 0 pda hua milta h



    //now  moving onto 2D array

    //we can find the 2d array using the index i.e. (row index ,column index);

    //formuls to convert the 2d arrays index into the 1d array ka index
    //index=c*i+j;
    //here c is the number of columns...





// vector <int> arr;
//     // how to use the linear search into an is_array
//     arr.push_back(10);
//        arr.push_back(20);
//           arr.push_back(30);
//              arr.push_back(40);
//                 arr.push_back(15);

//     int target = 70;
//     //this is new in vector how to get the size of an array
//     int n = arr.size();
//      aage sb array ki tarah same h and for loop ka use krk sb chize kr sakte ho tum







    // insertion kaie krte h vector main 

    // phela kaise h ki tum array ka size declare kr deya h to tum kaise insert kroge

    // in first array k tarah hi insert krenge
    // arr[2] = 20;

    // 2nd jb array ka size declared hi na ho tb tum kaise kroge 
    // by using a function clled pushback

    // arr.push_back(20);
    //     arr.push_back(10);

    //     agr vector k lst se kuch bhi hatana hoga to pop function ka use krenge

    // arr.pop_back;
 

    // how to declare an array using vectors
    // vector<int> arr;

    // how to give some size to an is_array
    // vector<int> arr(10);


    // how to give some size and declare what value i can gave to a whole array by size declaration

    // vector<int> arr(10,-1);





    return 0;
}