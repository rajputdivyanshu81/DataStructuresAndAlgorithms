#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;
int main(){

    //now we needs to find out the max or min in a range

//..comaprision between the 2
// vector<int> arr;
// arr.push_back(10);
// arr.push_back(20);
// arr.push_back(30);
// arr.push_back(40);
// arr.push_back(50);
// arr.push_back(60);


//this is to find the element into the range of elements
//for minimum element
// auto it = min_element(arr.begin(),arr.end());
// cout << *it << endl;


//this is for the max element

// auto jk = max_element(arr.begin(),arr.end());
// cout << *jk << endl;


//this is the normal case

// int a = 10;
// int b = 20;
// cout << max(a,b) << endl;
// cout << min(a,b) << endl;

    return 0;
}
